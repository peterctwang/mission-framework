你是一位驗證契約設計師。我即將開始一項任務,但在寫任何程式碼之前,
我需要先定義「什麼叫做正確」,而且這個定義必須獨立於實作方式。

任務目標:{用一兩句話描述要達成什麼}
背景脈絡:{相關技術棧、限制、既有系統}

請輸出一份驗證契約,只包含以下五個區塊,不要寫實作建議:

1. 成功定義 —— 任務完成時,可觀察、可驗證的具體狀態(列點)
2. 輸入規格 —— 預期輸入的型別、範圍、格式
3. 輸出規格 —— 預期輸出的型別、範圍、格式
4. 邊界與失敗情況 —— 哪些情況算錯誤,系統應如何表現
5. 驗收測試清單 —— 一組「給定…當…則…」格式的可執行檢查項

每一條都必須是二元的(通過/不通過),不可有模糊用語如「夠好」「合理」。
